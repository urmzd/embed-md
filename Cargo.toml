[package]
name = "embed-it"
version = "1.5.0"
edition = "2021"
description = "Embed source files into any text file"
license = "MIT"
repository = "https://github.com/urmzd/embed-it"

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
